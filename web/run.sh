#!/bin/bash

# Navigate to the web directory in the pi-velocity-core git repository
cd /path/to/pi-velocity-core/web

# Perform the desired operation (e.g., start a server, run tests, build documentation, etc.)
npm start
